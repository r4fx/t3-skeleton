pages.tx_contentdesigner_flexform.settings {
    #cObjectFlexFile = files/assets/plugins/content_designer/page-fields.xml

    tca = --div--;Settings,tx_contentdesigner_flexform

    cObject.sDEF {
        sheetTitle = Settings

        el {
            hidePageHeader {
                label = Hide page header
                config.type = check
            }
        }
    }

}