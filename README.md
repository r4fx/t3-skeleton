Site Package for the project "CMS Setup kit"
==============================================================

# Installation

* put `cms_setup_kit` folder into `packages` directory
* install via Composer using command `composer req vendor/cms-setup-kit:"@dev"`
* run `composer install`
