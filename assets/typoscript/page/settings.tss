page.10.settings {
    path {
        css = {$settings.path.css}
        js = {$settings.path.js}
        js-vendor = {$settings.path.js-vendor}
        images = {$settings.path.images}
        l10nFile = {$settings.path.l10nFile}
    }

    nav {
        mainNav =  {$settings.nav.mainNav}
        footerNav = {$settings.nav.footerNav}
    }

    googleAnaliticsUA =
    socialScriptsEnable = 0
}