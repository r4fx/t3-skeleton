page.10.settings {
    path {
        css = assets/styles/css
        js = assets/js
        vendor = assets/vendor
        images = assets/images
        l10nFile = assets/l10n/locallang.xlf
    }

    nav {
        mainNav =  {$settings.nav.mainNav}
        footerNav = {$settings.nav.footerNav}
        cookiesPageURL = {$settings.nav.cookiesPageURL}
    }

    titleEndSegment = TYPO3 Skeleton
    googleAnaliticsUA =
    socialScriptsEnable = 0
}